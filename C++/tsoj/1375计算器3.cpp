#include<iostream>
#include<sstream>
using namespace std;
int main()
{
	string s;
	while(cin>>s) 
	{
		stringstream stream;;//创建一个流  
		stream<<s;
		int len=s.length();
		try
		{
			if(cout<<(2+5)/2);
			else
				throw 1;
		}
		catch(...)
		{
			cout<<"None"<<endl;
		}
	}
}

